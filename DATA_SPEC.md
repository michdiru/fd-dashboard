# Логика данных дашборда ФД — справка для Claude Code

Этот файл описывает, откуда берутся данные, что означает каждая колонка и как
считаются показатели дашборда. Ссылайся на него при любой работе со скриптом
пересчёта — не изобретай маппинг департаментов заново, он уже подтверждён с
владельцем бизнеса.

## Файлы: два семейства

Каждый день A&A выгружает набор файлов с именами вида:

- `real_<dept>.<report_id>.csv` — транзакции по реализации (продажи) за департамент
- `st_<dept>.<report_id>.csv` — стартовые тренировки (СТ) за департамент

`<dept>` — короткий код: `tz`, `gp`, `bk`, `dc`, `mer`, `ms`, `sm`.
`<report_id>` (например, `304`) — служебный номер отчёта, меняется от выгрузки к
выгрузке и смысла не несёт. **Матчить файлы нужно по маске `real_*.csv` /
`st_*.csv`, а не по точному имени файла.**

## Кодировка — важно, не пропустить

Все файлы в **UTF-16LE с BOM** (`\xff\xfe`), разделитель — **табуляция**. Если
прочитать как UTF-8/Latin-1 — получится нечитаемая «разрядка» из кириллицы.

Правильное чтение в Python:
```python
with open(path, encoding='utf-16') as f:   # BOM сам укажет LE/BE
    reader = csv.reader(f, delimiter='\t')
```

## Схема `real_*.csv`

Колонки (первая строка — заголовок):

```
SectionName  ResourceName  FIO  Price  Qty  Summa  Name  TimeMes  HourValue  ServiceCode  DateValue
```

- `SectionName` — департамент, как он записан в A&A (сырое значение, см. таблицу маппинга ниже)
- `ResourceName` — тренер/сотрудник, оказавший услугу (нужно для рейтинга тренеров)
- `FIO` — имя клиента + ID клиента в скобках
- `Price`, `Qty`, `Summa` — цена, количество, сумма продажи. **Числа через запятую** как разделитель дробной части (`"1813,5"`) — перед `float()` заменить `,` → `.`
- `Name` — название услуги. По нему определяется ПТ: если `Name` содержит подстроку `"Персональн"` (без учёта регистра) — это персональная тренировка
- `DateValue` — полная дата-время сделки, формат `ДД.ММ.ГГГГ ЧЧ:ММ:СС`. По этому полю фильтруется отчётный период

## Схема `st_*.csv`

Колонки:
```
ResourceName  FIO  ExQnt  Qnt  Cr  DetailName  GroupLevel
```

- `ResourceName` — тренер, проводивший СТ
- `FIO` / `DetailName` — имя клиента (дублируют друг друга)
- `ExQnt` — количество проведённых СТ (обычно `1` на строку = одна тренировка). Суммировать по департаменту/тренеру → «Кол-во СТ»
- `Qnt` — `1`, если клиент купил после пробной тренировки, иначе `0`. Суммировать → «конверсия»
- `Cr` — на практике повторяет `Qnt`, отдельно не используется
- **Дат в этом файле нет.** Весь файл целиком = данные за текущий отчётный период (выгрузка A&A уже приходит «с начала месяца»). Фильтровать по датам здесь не нужно — если это предположение неверно, уточнить у того, кто настраивал выгрузку A&A

## Маппинг департаментов (сырое значение → каноническое имя)

| Канонический департамент | Файл(ы) | Сырые значения `SectionName`, которые сюда входят |
|---|---|---|
| ТЗ | `real_tz.*.csv` | `"Тренажерный зал"`, `"Общие услуги ФД"` (оба объединяются в ТЗ) |
| ГП | `real_gp.*.csv` | `"Аэробика"` |
| БК | `real_bk.*.csv` | `"Бойцовский клуб"`, `"Восточные единоборства"` (это один и тот же департамент — объединять) |
| ДЦ | `real_dc.*.csv` | `"Детский центр"` |
| Мероприятия ФД | `real_mer.*.csv` (и вообще любой файл) | **любое** значение `SectionName`, содержащее подстроку `"мероприятия"` (без учёта регистра) — суммировать все такие строки в одну статью, независимо от того, из какого файла они пришли |
| Массаж | `real_ms.*.csv` | `"Массаж"` |
| СМ | `real_sm.*.csv` | `"Спортивная медицина"` |

Для `st_*.csv` колонки `SectionName` нет вообще — департамент определяется
самим именем файла (суффиксом `bk`/`dc`/`gp`/`ms`/`sm`/`tz`).

## Что такое «отчётный период»

- **Текущий период** = с 1-го числа текущего месяца по сегодня включительно, фильтр по `DateValue` в `real_*.csv`.
- **Прошлый период** (для сравнения) = те же числа месяца, но годом ранее. Берётся из Google-таблицы, вкладка **«реализация за прошлые периоды»** (колонки: `Секция`, `Дата продажи`, `Доход` — одна строка = один департамент за один день). Суммировать `Доход` по департаменту за нужный диапазон дат, применяя тот же маппинг и то же правило объединения «мероприятий», что и выше (в таблице встречаются `"Мероприятия ФД"` и `"Мероприятия ФД ООО «Комета»"` — тоже одна статья).

## Как считаются показатели по департаменту

- **ПТ (шт)** — число строк в `real_<dept>.csv` за период, где `Name` содержит `"Персональн"`
- **Реализация, млн ₽** — `sum(Summa)` за период / 1 000 000
- **% плана (реализация)** — `Реализация / план_реализации[департамент] × 100`
- **Прошлый период, млн ₽** — см. выше, из Google-таблицы
- **Δ период** — `(Реализация_тек − Реализация_прошлая) / Реализация_прошлая × 100`
- **Кол-во СТ** — `sum(ExQnt)` из `st_<dept>.csv`
- **% плана (СТ)** — `Кол-во СТ / план_СТ[департамент] × 100`

## Планы (пока вводятся вручную)

Два списка значений, которые пока приходят от владельца бизнеса руками (позже —
из вкладки «планы» Google-таблицы, это отдельная задача на будущее):

- План по реализации, млн ₽, на департамент: ТЗ, ГП, БК, ДЦ, Мероприятия ФД, Массаж, СМ (+ ФД-итого = сумма всех)
- План по СТ, шт, на департамент: ТЗ, ГП, БК, ДЦ, СМ, Массаж (у «Мероприятия ФД» плана по СТ нет — там пробных тренировок не бывает)

Хранить эти цифры стоит в отдельном простом файле репозитория (например,
`data/plans.json`), который владелец бизнеса сможет менять сам через веб-интерфейс
GitHub, не трогая код.

## Рейтинг тренеров

Ранжировать тренеров **по количеству СТ** (`sum(ExQnt)` из `st_*.csv`), **не по
выручке**. Три отдельные таблицы:

1. **Топ-20** — объединяет `st_tz`, `st_gp`, `st_bk`, `st_dc` (все департаменты,
   кроме СМ и Массажа), группировка по `ResourceName`, суммирование, сортировка
   по убыванию, топ-20
2. **СМ** — только `st_sm.csv`, все тренеры по убыванию
3. **Массаж** — только `st_ms.csv`, все тренеры по убыванию

## Контрольные цифры для проверки скрипта

Если скрипт обрабатывает выгрузку за период 01–28.08.2026, он должен получить
примерно такие цифры (проверено вручную на реальных файлах пользователя):

| Департамент | ПТ | Реализация, млн ₽ | Прошлый период, млн ₽ | Кол-во СТ |
|---|---|---|---|---|
| ТЗ | 1 257 | 2.33 | 1.91 | 135 |
| ГП | 303 | 0.64 | 0.71 | 61 |
| БК | 46 | 0.10 | 0.07 | 30 |
| ДЦ | 28 | 0.08 | 0.11 | 5 |
| Мероприятия ФД | 0 | 0.10 | 0.17 | — |
| Массаж | 0 | 0.13 | 0.44 | 75 |
| СМ | 0 | 0.04 | 0.04 | 150 |
| **ФД — итого** | **1 634** | **3.42** | **3.46** | **456** |

Если пересчёт даёт заметно другие цифры — сначала проверить кодировку и
маппинг департаментов, а не логику агрегации.
